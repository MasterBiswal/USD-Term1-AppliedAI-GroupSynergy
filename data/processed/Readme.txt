After placing the K9.data and K9.instance.tags in the raw folder, please run the codes as mentioned below :

1. Go to load_check.ipynb file inside notebooks and run each code cell from the beginning.
	Result : It will generate the cleaned_tags.csv file inside data->processed folder.

2. After completion, Go to 01_data_cleaning.ipynb file inside notebooks and run each code cell from the beginning.
	Result : It will generate cleaned_features.csv, encoded_labels.npy, and tag_classes files inside data->processed folder.